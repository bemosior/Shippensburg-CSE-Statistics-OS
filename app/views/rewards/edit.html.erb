<% if defined? @reward.uuid %>
  <% if @reward.used %>
    <h1>Thank you.</h1>
    <p>You have already submitted your email for consideration. You will be contacted in the near future if you win.</p>
  <% else %>
    <h1><%= @reward.uuid %></h1>
    <%= simple_form_for @reward do |f| %>
      <%= f.input :email %>
      <%= f.input :uuid, as: :hidden %>
      <%= f.submit t('reward.submit'), class: 'button' %>
    <% end %>
  <% end %>

<% else %>
  <h1>Woops.</h1>
  <p>Something weird happened with the link. If you believe that you shouldn't be seeing this page, email me: bemosior@gmail.com</p>
<% end %>